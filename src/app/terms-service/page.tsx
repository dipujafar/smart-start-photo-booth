import React from 'react';
import TermsPageContainer from './components/TermsPageContainer';

const TermsServicePage = () => {
    return (
       <TermsPageContainer></TermsPageContainer>
    );
};

export default TermsServicePage;